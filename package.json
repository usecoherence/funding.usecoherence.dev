{
  "name": "funding.usecoherence.dev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "site:dev": "npm --prefix website run dev",
    "site:build": "npm --prefix website run build",
    "deploy": "npm --prefix website run build && node --import ./deploy/load-env.mjs ./deploy/deploy-pgs.mjs",
    "dns:setup": "node --import ./deploy/load-env.mjs deploy/setup-cloudflare-dns.mjs",
    "db:init": "node --experimental-strip-types grant/init-db.ts",
    "cli": "tsx grant/cli.ts",
    "check:types": "tsc --noEmit",
    "check:lint": "eslint grant/",
    "check:format": "prettier --check grant/",
    "check": "npm run check:types && npm run check:lint && npm run check:format"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@eslint/js": "^10.0.1",
    "@types/better-sqlite3": "^9.6.0",
    "@types/node": "^26.5.0",
    "better-sqlite3": "^13.0.3",
    "commander": "^15.0.0",
    "drizzle-kit": "^0.31.10",
    "drizzle-orm": "^0.45.2",
    "eslint": "^10.10.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.9.6",
    "tsx": "^4.23.13",
    "typescript": "~5.9.0",
    "typescript-eslint": "^8.70.0"
  },
  "engines": {
    "node": ">=22"
  }
}
